# SENT-1103 — aria-label pass on login, dashboard, alert queue

| Field | Value |
|-------|-------|
| **Type** | Story |
| **Epic** | SENT-E11 Portfolio Scale and NFR Hooks |
| **Priority** | High |
| **Story Points** | 3 |
| **Labels** | `frontend`, `implementation` |
| **Paired QA ticket** | [SENT-1103-QA](./SENT-1103-QA.md) |

---

## Summary

aria-label pass on login, dashboard, alert queue.

---

## Description

**As a** QA engineer  
**I want** `aria-label` attributes mirroring `data-testid` on primary interactive elements across the login, dashboard, and alert queue pages  
**So that** accessibility audits pass and Selenium tests have an alternative stable locator strategy via ARIA attributes

---

## Acceptance criteria

### AC1 — aria-label mirrors data-testid on primary actions for P1–P3

- [ ] Every element on pages P1 (Login), P2 (Dashboard), and P3 (Alert queue) that has a `data-testid` attribute also has an `aria-label` with the same value
- [ ] Verified against the pages defined in CONSTITUTION §7

---

## Technical notes

Optional a11y practice.

---

## Out of scope

- Any files under repository root `tests/` (see paired QA ticket)
- Developer unit tests inside `backend/` or `frontend/`

---

## Definition of Done

- [ ] Acceptance criteria met
- [ ] `data-testid` hooks on new UI controls (if frontend)
- [ ] OpenAPI updated (if API)
- [ ] No test modules added outside `tests/`
- [ ] Ticket ACs and DoD marked `[x]`, `Status: Done` added to metadata
- [ ] `README.md` App implementation status updated for this ticket
- [ ] Epic checklist ticked only if this was the last story in the epic
