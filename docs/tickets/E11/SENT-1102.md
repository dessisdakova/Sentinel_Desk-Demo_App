# SENT-1102 — Locustfile sample for ingest and list

| Field | Value |
|-------|-------|
| **Type** | Story |
| **Epic** | SENT-E11 Portfolio Scale and NFR Hooks |
| **Priority** | High |
| **Story Points** | 3 |
| **Labels** | `docs`, `implementation` |
| **Paired QA ticket** | [SENT-1102-QA](./SENT-1102-QA.md) |

---

## Summary

Locustfile sample for ingest and list.

---

## Description

**As a** QA engineer  
**I want** a sample Locustfile that exercises the ingest and alert list endpoints at load  
**So that** I have a starting point for performance testing against the NFR targets without writing Locust tasks from scratch

---

## Acceptance criteria

### AC1 — Locustfile location and tasks

- [ ] A `locustfile.py` is placed at `scripts/locustfile.py` (or `docs/perf/locustfile.py`) with at least two Locust tasks: one that POSTs to the ingest endpoint and one that GETs the alert list with filters

### AC2 — NFR targets documented in README

- [ ] README (or a `docs/PERFORMANCE.md` section) documents the NFR targets from CONSTITUTION §9: `p95 < 500ms` at 20 concurrent users on the alert queue list, and `100 ingest/min` throughput

---

## Technical notes

No load tests in CI required.

---

## Out of scope

- Any files under repository root `tests/` (see paired QA ticket)
- Developer unit tests inside `backend/` or `frontend/`

---

## Definition of Done

- [ ] Acceptance criteria met
- [ ] `data-testid` hooks on new UI controls (if frontend)
- [ ] OpenAPI updated (if API)
- [ ] No test modules added outside `tests/`
- [ ] Ticket ACs and DoD marked `[x]`, `Status: Done` added to metadata
- [ ] `README.md` App implementation status updated for this ticket
- [ ] Epic checklist ticked only if this was the last story in the epic
